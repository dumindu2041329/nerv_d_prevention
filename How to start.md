### Start the backend
```bash
cd backend
# Install dependencies
dart pub get
# Start the server
dart run lib/server.dart
```

### Start the frontend
```bash
# Start the frontend
cd ..
# Install dependencies
flutter pub get
# Start the app
flutter run -d chrome
```