name: nerv_backend
description: Backend proxy for NERV Disaster Prevention App
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  dio: ^5.4.0
  dotenv: ^4.2.0

dev_dependencies:
  mockito: ^5.4.4
  build_runner: ^2.4.8
