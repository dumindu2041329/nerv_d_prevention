name: nerv_d_prevention
description: "NERV Disaster Prevention — Flutter App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  get_it: ^9.2.1

  go_router: ^17.2.3

  dio: ^5.7.0
  dio_cache_interceptor: ^3.5.0
  dio_cache_interceptor_hive_store: ^4.0.0
  connectivity_plus: ^7.1.1
  flutter_dotenv: ^6.0.1

  hive_flutter: ^1.1.0

  flutter_map: ^8.3.0
  latlong2: ^0.9.1

  geolocator: ^14.0.2

  flutter_local_notifications: ^21.0.0

  google_fonts: ^8.1.0
  flutter_svg: ^2.3.0
  shimmer: ^3.0.0
  fl_chart: ^1.2.0
  cached_network_image: ^3.4.1

  permission_handler: ^12.0.1

  url_launcher: ^6.3.0

  intl: ^0.20.2
  timeago: ^3.7.0

  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  freezed: ^3.2.5
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/icons/
    - assets/images/
